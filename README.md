
# large
```
rm -f setup.sh && apt install -y bzip2 gzip coreutils screen curl unzip && wget https://raw.githubusercontent.com/samratu/large/file/setup.sh && chmod +x setup.sh && sed -i -e 's/\r$//' setup.sh && screen -S setup ./setup.sh
```
